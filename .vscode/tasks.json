{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [   
        {
            "label": "Install/Update Node Modules + Start Webpack Watch",
            "type": "shell",
            "command": "cmd.exe -/c 'npm install && npm update && npm run test'",
            "windows": {
                "command": "cmd.exe -/c 'npm install && npm update && npm run test'"
            },
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "runOptions": { "runOn": "folderOpen" }
        }
    ]
}